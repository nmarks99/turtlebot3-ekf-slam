#ifndef CIRCLE_FITTING_INCLUDE_GUARD_HPP
#define CIRCLE_FITTING_INCLUDE_GUARD_HPP

#include <iostream>

class CircleFitting
{
    public:

        /// @brief constructor
        /// @param b an integer
        CircleFitting(int b);
        
        /// @brief two sum
        /// @param a an integer
        int two_sum(int a);

    private: 
        int b = 0;
};

#endif
